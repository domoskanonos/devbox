# docker-compose
## start devbox
    docker-compose up
## shutdown devbox
    docker-compose down
## start devbox in deamond mode
    docker-compose up -d
# docker commands
    docker container prune
    docker image prune -a